const config = {
    host: 'localhost',
    user: 'root',
    password: 'password',
    database: 'freshstock',
    port: 3306,
    waitForConnections: true,
    connectionLimit: 10,
    queueLimit: 0
};

module.exports = config;